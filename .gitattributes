# 저장소 전역 줄바꿈 정규화 (SVG/텍스트 CRLF 경고 방지)
* text=auto eol=lf
*.svg text eol=lf
*.md  text eol=lf
*.yml text eol=lf

# 빌드 산출물 — 원본은 web/guide-src/parts/**
# (GitHub PR에서 diff를 접어두고 언어 통계에서도 제외. 로컬 git diff 는 그대로 보임)
web/public/python-guide/index.html linguist-generated=true
