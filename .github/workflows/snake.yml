name: 🐍 Generate Snake Animation

on:
  # 매일 자정(UTC) 자동 실행
  schedule:
    - cron: "0 0 * * *"
  # 수동 실행 허용
  workflow_dispatch:
  # main 브랜치 push 시 실행
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Generate github-snake
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            dist/github-snake.svg?color_snake=%2300ff41&color_dots=%230d0208,%23003b00,%23008f11,%2300c020,%2300ff41
            dist/github-snake-dark.svg?color_snake=%2300ff41&color_dots=%230d0208,%23003b00,%23008f11,%2300c020,%2300ff41

      - name: Push to output branch
        uses: crazy-max/ghaction-github-pages@v5
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
